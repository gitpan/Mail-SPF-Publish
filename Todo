TODO list for Perl module Mail::SPF::Publish

- IPv6 support
- Consolidate v4 address ranges that fall on byte boundaries
- Tests
- Multi-homed servers should work, but you might get extra deny records laying around with them.

